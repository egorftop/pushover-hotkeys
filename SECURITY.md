# Security

Do not add Pushover App Token, User Key, Group Key, `%LOCALAPPDATA%\PushoverHotkeys\settings.dat`, or screenshots containing them to issues, pull requests, or commits.

If a Pushover key is exposed, revoke it in Pushover immediately and create a replacement key. Do not report the exposed value in a public issue.
